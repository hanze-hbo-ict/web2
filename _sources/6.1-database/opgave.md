# Opgave


Voor het project is het handig om nu op z'n minst de klasse te maken die de `ConnectionInterface` implementeert. Zorg ervoor dat je deze instantie in je [*composition root*](../5.1-di/compositionroot.md) aanmaakt en dat deze bij als *service* beschikbaar wordt gesteld in de klassen die een database nodig hebben.